# @deep-tui/plugin-theme-default

Registers Deep TUI's default Midnight theme. The theme ID, label, and design
tokens can be overridden through plugin configuration.

```json
{ "use": "@deep-tui/plugin-theme-default" }
```
