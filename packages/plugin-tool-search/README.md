# @flect/plugin-tool-search

Adds bounded `find_files` and `search_text` tools without following symlinks.

Search skips ignored and binary files, supports glob/literal/regular-expression
queries, reports structured line/column previews, and enforces result, file,
query, cancellation, and time bounds.
