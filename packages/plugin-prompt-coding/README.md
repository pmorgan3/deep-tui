# @deep-tui/plugin-prompt-coding

Registers Deep TUI's default coding-agent system prompt. Supply `config.text`
to replace the built-in prompt without replacing the surrounding agent loop.

```json
{
  "use": "@deep-tui/plugin-prompt-coding",
  "config": { "text": "Work carefully and explain verification." }
}
```
