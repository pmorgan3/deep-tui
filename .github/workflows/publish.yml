name: Publish npm packages

on:
  push:
    branches:
      - main
    paths:
      - packages/*/package.json
  workflow_dispatch:
    inputs:
      tag:
        description: npm distribution tag
        required: true
        default: next
        type: choice
        options:
          - next
          - latest
      dry_run:
        description: Build and pack without publishing
        required: true
        default: true
        type: boolean

permissions:
  contents: read
  id-token: write

concurrency:
  group: npm-publish
  cancel-in-progress: false

jobs:
  publish:
    if: github.repository == 'pmorgan3/deep-tui' && github.ref == 'refs/heads/main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0
      - uses: pnpm/action-setup@v4
        with:
          version: 10.33.0
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org
          package-manager-cache: false
      - name: Use an OIDC-capable npm CLI
        run: npm install --global npm@12
      - run: pnpm install --frozen-lockfile
      - run: pnpm check
      - name: Check package tarballs
        if: github.event_name == 'workflow_dispatch' && inputs.dry_run
        run: pnpm run publish:packages -- --tag "${{ inputs.tag }}" --dry-run
      - name: Publish version changes
        if: github.event_name == 'push'
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: pnpm run publish:packages -- --tag latest --changed-since "${{ github.event.before }}"
      - name: Publish package tarballs manually
        if: github.event_name == 'workflow_dispatch' && !inputs.dry_run
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
        run: pnpm run publish:packages -- --tag "${{ inputs.tag }}"
